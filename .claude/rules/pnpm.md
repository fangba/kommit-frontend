# Gestionnaire de paquets — pnpm

Sur ce repo, le gestionnaire de paquets est **pnpm**, tout le temps, sans exception : jamais `npm`/`yarn`, et jamais de `package-lock.json` ou `yarn.lock` à côté de `pnpm-lock.yaml`.
